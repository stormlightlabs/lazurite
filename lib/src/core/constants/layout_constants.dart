import 'package:flutter/material.dart';

/// Shared layout constants to keep UI elements aligned consistently.
const double kRefreshIndicatorEdgeOffset = kToolbarHeight;

/// Distance the refresh indicator travels before settling under the app bar.
const double kRefreshIndicatorDisplacement = kToolbarHeight + 16;
