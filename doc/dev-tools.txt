*dev-tools.txt*                                                 ATProto DevTools
================================================================================
DevTools - Scope & safety boundaries                                 *dev-scope*
================================================================================

Read-only by default (MVP):

- View your own repo (logged-in DID) + optionally view other public repos.

Hard rules:

- Never show or export auth secrets (tokens/DPoP keys) stored in secure storage.
- Default to "My DID". "Other DID" requires an explicit toggle and a
  confirmation.
- Record JSON rendering must be robust to hostile / weird shapes (defensive UI).

Host routing:

- These are PDS repo endpoints; call the user’s PDS directly:
  - describeRepo / listRecords / getRecord.
    (Do NOT use `atproto-proxy` here; that header is for proxying to other
    services, e.g. labelers/chat.)

================================================================================
DevTools - Data + endpoints (canonical)                                *dev-api*
================================================================================

Repo introspection:

- com.atproto.repo.describeRepo (GET) - lists collections for a repo.
  Query: repo=<did|handle>

Collection listing:

- com.atproto.repo.listRecords (GET) - list records in a collection.
  Query: repo=<did|handle>
         collection=<nsid>
         limit=<n>
         cursor=<cursor?>
         rkeyStart/rkeyEnd?
         reverse?

Single record:
- com.atproto.repo.getRecord (GET) - fetch record by repo+collection+rkey.
  Query: repo=<did|handle>
         collection=<nsid>
         rkey=<rkey>
         cid?<optional>

Advanced proof (optional later):
- com.atproto.sync.getRecord (GET) - "prove existence/non-existence" blocks.

Mutation:
- com.atproto.repo.createRecord (POST)
- com.atproto.repo.putRecord (POST)
- com.atproto.repo.deleteRecord (POST)
- com.atproto.repo.applyWrites (POST; batched transaction)

================================================================================
DevTools - UX: desktop vs mobile                                        *dev-ux*
================================================================================

Desktop (high visibility / inspector):
- 3-pane layout:
  - Left: Collections (search + pinned)
  - Middle: Record list (paged, sortable, filterable)
  - Right: Record inspector (JSON tree + metadata + actions)

Mobile (usable drill-down):
- Screen stack:
  1. DevTools Home (My DID + optional Other DID + recent)
  2. Collections list (search + pinned)
  3. Record list (infinite scroll + filter chip row)
  4. Record detail (collapsible JSON + copy/share)

Universal actions (every platform):
- Copy: AT URI, CID, rkey, collection NSID
- Export: JSON (share sheet on mobile, save-to-file on desktop)

================================================================================
DevTools - Storage (Drift)                                         *dev-storage*
================================================================================

Keep devtools cache-light (bounded, LRU):
  - store “recently viewed records” + “pinned collections” locally
  - cache record JSON for offline inspection (bounded by size + LRU)

Tables:
- dev_recent_records(
      uri TEXT PRIMARY KEY,
      did TEXT NOT NULL,
      collection TEXT NOT NULL,
      rkey TEXT NOT NULL,
      cid TEXT,
      viewedAt INTEGER NOT NULL,
      json TEXT,                 "bounded cache; null if too large"
      jsonSize INTEGER
  )
- dev_pins(
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      kind TEXT NOT NULL,        "collection|record"
      key TEXT NOT NULL,         "collection nsid OR record uri"
      createdAt INTEGER NOT NULL
  )
- dev_settings(
      id INTEGER PRIMARY KEY CHECK (id=1),
      enabled INTEGER NOT NULL,
      allowOtherRepos INTEGER NOT NULL,
      maxCachedJsonBytes INTEGER NOT NULL
  )

Indexes:
- dev_recent_records(did, viewedAt DESC)
- dev_recent_records(did, collection, viewedAt DESC)

Rationale:
- listRecords is already paged and cheap; caching is for "instant back" +
  offline peek.

================================================================================
DevTools - State + routing glue (Riverpod + go_router)              *dev-router*
================================================================================

Providers:
- Provider<DevtoolsRepository> (wraps XrpcClient + Drift DAOs)
- AsyncNotifierProvider
  <DevtoolsRepoController, RepoState> (describeRepo)
- AsyncNotifierProviderFamily
  <DevtoolsRecordsController, RecordsState, (did, collection, filter)>
- AsyncNotifierProviderFamily
  <DevtoolsRecordController, RecordState, (did, collection, rkey)>

Routes:
- /devtools
- /devtools/repo/:did
- /devtools/repo/:did/collections/:nsid
- /devtools/repo/:did/collections/:nsid/records/:rkey
- /devtools/record?uri=<at-uri>   (optional convenience)

Gating:
- Devtools routes hidden unless devtools.enabled == true.
- On mobile: hide "Other DID" unless allowOtherRepos == true.

================================================================================
Devtools - Record Explorer                                      *dev-record-exp*
================================================================================
Developer Mode plumbing + entry points
--------------------------------------------------------------------------------

Tasks (core):
- [ ] Add Settings → "Developer Mode" toggle (persist in dev_settings).
- [ ] Add hidden/advanced enable gesture (optional) mirroring Bluesky’s style:
  long-press version number / build row to reveal dev toggle.
- [ ] Add /devtools route group behind the toggle.
- [ ] Add "Copy my DID" action (top of DevTools Home).

Screens:
- DevToolsHomePage
  - shows: My DID, My PDS host, quick actions
    (copy DID, open collections, recent)
- DevToolsSettingsSection (inside SettingsPage)

Widgets:
- DevtoolsGateBanner ("Developer mode is enabled" + disable button)
- CopyIconButton (generic, reusable: copies value + toast)
- LabeledValueRow (label + monospaced value + copy)

Tests:
- [ ] Widget test: devtools routes hidden when disabled.
- [ ] Provider test: dev_settings toggles drive navigation availability.

Definition of done:
- You can enable dev mode, navigate to DevTools Home, and copy your DID.

--------------------------------------------------------------------------------
Repo overview + collections browser (describeRepo)
--------------------------------------------------------------------------------

API tasks:
- [ ] Implement com.atproto.repo.describeRepo client call.
- [ ] Parse + display collections list (NSIDs).
- [ ] Support repo selector:
  - default: logged-in DID
  - optional: input DID/handle if allowOtherRepos is enabled.

Screens:
- RepoOverviewPage (optional; can fold into DevToolsHomePage)
- CollectionsPage(did)
  - search field + pinned section + full list
  - tap → RecordsPage(did, nsid)

Widgets:
- CollectionListTile (nsid + record count if available later)
- PinButton (pin/unpin collection)
- SearchField (Material 3, debounced)

Storage tasks:
- [ ] Persist pinned collections in dev_pins.

Tests:
- [ ] describeRepo response → collections sorted + filterable.
- [ ] collections list renders long NSIDs without overflow.

Definition of done:
- Collections list is correct for your DID and navigates to record listing.

--------------------------------------------------------------------------------
Record list (listRecords) with paging + filters
--------------------------------------------------------------------------------

API tasks:
- [ ] Implement com.atproto.repo.listRecords with cursor paging.
- [ ] Implement list state:
  - initial load
  - load next page (cursor)
  - refresh (reset cursor)
- [ ] Add basic filters (client-side, MVP):
  - rkey prefix filter
  - "has blobs" (best-effort: scan record for blob refs)
  - "recently changed" (if server provides timestamps in record view;
    otherwise local)

Screens:
- [ ] RecordsPage(did, nsid)
  - list with infinite scroll
  - filter row (chips)
  - item tap → RecordDetailPage

Desktop enhancement:
- [ ] RecordsPage becomes middle pane in split layout.

Widgets:
- [ ] RecordRow (rkey, uri, cid, compact preview)
- [ ] CursorPagingFooter (loading / error / end)
- [ ] FilterChipsRow
- [ ] InlineRetryTile

Storage tasks:
- [ ] Add "recent record views" when user opens a record
      (write dev_recent_records).

Tests:
- [ ] Provider tests: cursor progression + dedupe by (uri).
- [ ] UI test: scrolling triggers next-page call exactly once per threshold.

Definition of done:
- You can scroll through records in a collection without duplicates and with
  stable paging.

--------------------------------------------------------------------------------
Record inspector (getRecord) + export/copy tooling
--------------------------------------------------------------------------------

API tasks:
- [ ] Implement com.atproto.repo.getRecord.
- [ ] Support optional "cid" fetch (if user wants a specific version) when
      available.
- [ ] Add "Proof" panel (optional, behind advanced toggle):
  com.atproto.sync.getRecord blocks fetch.

Screens:
- [ ] RecordDetailPage(did, nsid, rkey)
  - Header: AT URI + CID + timestamps (if present)
  - Body: JSON tree viewer
  - Actions: copy URI/CID/JSON, export JSON, open external

Desktop:
- [ ] Right pane inspector with:
  - JSON tree
  - raw JSON tab
  - "copy path" for selected JSON node (nice-to-have)

Widgets:
- [ ] JsonTreeView (collapsible nodes; must handle big objects)
- [ ] InspectorHeader (monospace URI + quick-copy icons)
- [ ] ExportButton (desktop: save file; mobile: share)
- [ ] JsonSearchBar (search within JSON text; desktop-first)

Storage tasks:
- [ ] Cache JSON to dev_recent_records.json up to maxCachedJsonBytes, LRU evict.

Tests:
- [ ] large nested JSON renders with collapsible nodes.
- [ ] export produces valid UTF-8 JSON and matches fetched payload.

Definition of done:
- Any record can be inspected, copied, and exported reliably on mobile and desktop.

--------------------------------------------------------------------------------
Manage records (write mode) - desktop-first, gated
--------------------------------------------------------------------------------

"Login to manage records in your repository"
It is intentionally gated because it can brick user data if sloppy.

Gate UX:
- Toggle: "Enable record editing (dangerous)"
- Require: biometric / device auth re-check (mobile) OR
           typed confirmation phrase (desktop)
- Scope: only logged-in DID (no editing other repos)

API tasks:
- [ ] Create: com.atproto.repo.createRecord (new rkey or server-generated).
- [ ] Update/Upsert: com.atproto.repo.putRecord.
- [ ] Delete: com.atproto.repo.deleteRecord.
- [ ] Batch ops (advanced): com.atproto.repo.applyWrites (transaction).

Screens:
- [ ] RecordEditorPage
  - JSON editor (text)
  - validate / format
  - dry-run diff preview (client-side)
- [ ] ApplyWritesReviewDialog (shows creates/updates/deletes summary)

Widgets:
- [ ] JsonCodeEditor (desktop: code editor-like; mobile: basic text editor)
- [ ] DiffViewer (desktop; minimal unified diff)
- [ ] DangerousActionButton (explicit styling + confirm step)

Tests:
- [ ] Contract tests against a test PDS (or recorded fixtures):
  - create → list shows new record
  - put → get shows updated record
  - delete → get fails / not found
- [ ] Safety tests:
  - cannot edit when gate is off
  - cannot edit repo != logged-in DID

Definition of done:
- You can safely create/update/delete your own records with clear review UX.

--------------------------------------------------------------------------------
Definition of Done
--------------------------------------------------------------------------------

Read-only explorer is "done" when:
- Dev mode toggles the entire feature set on/off.
- Collections are discovered via describeRepo.
- Records list is paged via listRecords with cursor.
- Record inspector loads via getRecord and exports/copies correctly.
- Desktop inspector + mobile drill-down both feel first-class (not "ported UI").

Write mode is "done" when:
- All mutation endpoints are wired with explicit gating and review UX.
- No accidental cross-repo editing is possible.
