*roadmap.txt*                         Material You Bluesky Client (Flutter/Dart)

================================================================================
J. Composer + local drafts + publish pipeline                           *bsky-J*
================================================================================

Status: Foundation complete, screens and navigation needed

Endpoints (write):
- com.atproto.repo.uploadBlob (POST) - registered
- com.atproto.repo.createRecord (POST) - registered

Foundation:
- [x] Database schema (Drafts + DraftMedia tables)
- [x] Data access layer (DraftsDao with full CRUD)
- [x] Domain models (Draft, DraftMediaAttachment, statuses)
- [x] Repository (DraftRepository with complete publish pipeline)
- [x] Riverpod providers (draftRepository, drafts, draft)
- [x] Core widgets:
      - [x] ComposerTextField (multi-line with character counter)
      - [x] MediaPickerRow (horizontal media list)
      - [x] DraftStatusChip (status badges)
      - [x] PublishButton (loading + disabled states)
      - [x] UploadProgressTile (per media progress)
      - [x] InlineComposerLauncher (inline trigger widget)
- [x] Routes defined (/compose, /drafts, /drafts/:draftId)

Phase 1 - Core Composer Screen (HIGH):
- [ ] Create ComposerScreen widget:
      - [ ] Modal route presentation (full screen)
      - [ ] Accept draftId parameter (new vs edit)
      - [ ] Create new draft on init (if draftId null)
      - [ ] Load existing draft (if draftId provided)
      - [ ] Integrate ComposerTextField widget
      - [ ] Integrate MediaPickerRow widget
      - [ ] Integrate PublishButton widget
      - [ ] Character counter with grapheme clusters (characters package)
      - [ ] Character limit enforcement (300 graphemes)
      - [ ] Visual warning near limit (20 remaining)
      - [ ] Error state over limit (disable publish)
- [ ] Implement autosave:
      - [ ] Debounce text changes (500ms)
      - [ ] Update draft on text change
      - [ ] Immediate save on media add/remove
      - [ ] Save on app backgrounding
      - [ ] Cancel pending saves on dispose
- [ ] Image picker integration:
      - [ ] Gallery selection (image_picker)
      - [ ] Camera capture
      - [ ] Max 4 images limit
      - [ ] JPEG/PNG/WebP support
      - [ ] Add media to draft
      - [ ] Remove media from draft
- [ ] Publish flow:
      - [ ] Call DraftRepository.publishDraft()
      - [ ] Show loading state
      - [ ] Handle success (pop modal)
      - [ ] Handle errors (show message, preserve draft)
      - [ ] Update draft status during publish
- [ ] Cancel flow:
      - [ ] Save draft on cancel button
      - [ ] Pop modal
      - [ ] Confirm if unsaved changes
- [ ] Router configuration:
      - [ ] Add /compose route to router
      - [ ] Configure as modal route
      - [ ] Accept query params (draftId, replyTo, quoteTo)

Phase 2 - Draft List and Management (HIGH):
- [ ] Create DraftListScreen widget:
      - [ ] Load drafts from draftsProvider
      - [ ] Sort by updatedAt (most recent first)
      - [ ] Display draft preview cards:
            - [ ] Text preview (first 2 lines)
            - [ ] Media count badge if present
            - [ ] Reply/quote indicator icons
            - [ ] Status chip (draft/publishing/failed/posted)
            - [ ] Timestamp (relative time)
            - [ ] Error message for failed drafts
      - [ ] Empty state when no drafts
      - [ ] Pull to refresh (cleanup old posted)
- [ ] Draft list interactions:
      - [ ] Tap card to edit (navigate to /compose?draftId=xxx)
      - [ ] Swipe to delete draft
      - [ ] Retry button for failed drafts
      - [ ] Delete confirmation dialog
- [ ] Create DraftRecoverySheet:
      - [ ] Detect crashed publishes on app start
      - [ ] Show modal with failed draft
      - [ ] Retry or delete options
      - [ ] Auto-dismiss if successful
- [ ] Router configuration:
      - [ ] Add /drafts route to router
      - [ ] Add /drafts/:draftId route
      - [ ] Configure navigation to composer with params

Phase 3 - Alt Text and Media Polish (MEDIUM):
- [ ] Create AltTextEditorSheet modal:
      - [ ] Text field for alt text
      - [ ] Character limit (1000 chars)
      - [ ] Save button
      - [ ] Cancel button
      - [ ] Accessibility guidance text
- [ ] Integrate alt text editor:
      - [ ] Tap media thumbnail to open sheet
      - [ ] Pass media ID to sheet
      - [ ] Update draft media alt text
      - [ ] Display alt text indicator on thumbnails
- [ ] Image compression:
      - [ ] Compress before upload (flutter_image_compress)
      - [ ] Target max dimension: 2000px
      - [ ] Quality: 85% JPEG
      - [ ] Background isolate compression
      - [ ] Show compression progress
- [ ] Upload progress improvements:
      - [ ] Per-image upload progress
      - [ ] Retry failed uploads
      - [ ] Cancel in-progress uploads
      - [ ] Update UploadProgressTile with retry

Phase 4 - Reply and Quote Posts (MEDIUM):
- [ ] Create ReplyContextCard widget:
      - [ ] Display parent post author
      - [ ] Display parent post text (truncated)
      - [ ] Show thread indicator line
      - [ ] Compact card layout
- [ ] Create QuotePostCard widget:
      - [ ] Display quoted post author
      - [ ] Display quoted post text
      - [ ] Show quoted post media (if any)
      - [ ] Card border styling
- [ ] Composer reply support:
      - [ ] Accept replyTo parameter (post URI)
      - [ ] Fetch parent post data
      - [ ] Extract reply references (parent/root URI+CID)
      - [ ] Create draft with reply refs
      - [ ] Display ReplyContextCard
      - [ ] Include reply in post record
- [ ] Composer quote support:
      - [ ] Accept quoteTo parameter (post URI)
      - [ ] Fetch quoted post data
      - [ ] Extract quote reference (URI+CID)
      - [ ] Create draft with quote ref
      - [ ] Display QuotePostCard
      - [ ] Include quote embed in post record
      - [ ] Support recordWithMedia (quote + images)

Phase 5 - Rich Text (Mentions and Links) (MEDIUM):
- [ ] Create FacetParser utility:
      - [ ] Detect @mentions in text
      - [ ] Resolve handles to DIDs (lookup via API)
      - [ ] Detect URLs in text
      - [ ] Calculate byte offsets (UTF-8 encoding)
      - [ ] Build facet array
      - [ ] Handle unicode and emoji correctly
- [ ] Integrate facet parsing:
      - [ ] Parse text on publish
      - [ ] Store facets in facetsJson
      - [ ] Include facets in post record
      - [ ] Visual styling for mentions
      - [ ] Visual styling for links
- [ ] Link card preview:
      - [ ] Detect URLs in text
      - [ ] Fetch link metadata (unfurling)
      - [ ] Display link card preview
      - [ ] Include external embed

Phase 6 - Global FAB and Polish (HIGH):
- [ ] Create global FAB widget:
      - [ ] Extended FAB with "Post" label
      - [ ] Pen/edit icon
      - [ ] Primary color styling
      - [ ] Elevation shadow
- [ ] Integrate FAB in app scaffold:
      - [ ] Show on: Home, Feeds, Search, Profile
      - [ ] Hide on: Composer, Login, Drafts, Settings
      - [ ] Fixed bottom-right position
      - [ ] Smooth show/hide animation
      - [ ] Tap navigates to /compose
      - [ ] Create new draft automatically
- [ ] Keyboard shortcuts:
      - [ ] Ctrl/Cmd+N to open composer
      - [ ] Esc to cancel composer
      - [ ] Ctrl/Cmd+Enter to publish
- [ ] Performance optimization:
      - [ ] Ensure text input <16ms render
      - [ ] Optimize autosave debouncing
      - [ ] Async database writes
      - [ ] Image thumbnail caching
      - [ ] Smooth modal transitions
- [ ] Analytics events:
      - [ ] Composer opened
      - [ ] Draft saved
      - [ ] Post published
      - [ ] Media attached
      - [ ] Publish failed

Tests:
- [ ] Unit tests:
      - [ ] Draft creation and updates
      - [ ] Media attachment operations
      - [ ] Publish pipeline (mock API)
      - [ ] Post record building
      - [ ] Error handling and retries
      - [ ] Facet parsing and byte offsets
      - [ ] Character counting (grapheme clusters)
- [ ] Widget tests:
      - [ ] ComposerScreen interactions
      - [ ] DraftListScreen rendering
      - [ ] AltTextEditorSheet input
      - [ ] ReplyContextCard display
      - [ ] QuotePostCard display
      - [ ] Character counter states
      - [ ] Maintain existing widget tests

Deliverables:
- [ ] Global FAB visible on main screens
- [ ] Composer screen with text input and media
- [ ] Character limit enforcement (300 graphemes)
- [ ] Image attachments (up to 4) with alt text
- [ ] Draft autosave and persistence
- [ ] Draft list with status indicators
- [ ] Retry failed publishes
- [ ] Reply posts with parent context
- [ ] Quote posts with embed preview
- [ ] Rich text (mentions and links)
- [ ] Offline drafts remain editable
- [ ] Publish failures preserve drafts
- [ ] Test coverage >95%
- [ ] Smooth performance (<16ms text input)

================================================================================
K. Settings & Preferences                                               *bsky-K*
================================================================================

Context:
- Settings route exists but shows placeholder screen (app/router.dart:273-307).
- Only saved feeds preference currently synced with Bluesky.
- Theme mode stored in-memory without persistence.
- Missing content moderation, labelers, muted words, and other Bluesky prefs.
- No about screen or comprehensive local app settings.

Endpoints (already registered):
- app.bsky.actor.getPreferences (GET, requires auth)
- app.bsky.actor.putPreferences (POST, requires auth)

Tasks:
- [x] Phase 1 - Local Settings Foundation (HIGH) [partial]:
      - [x] Create LocalSettings table and LocalSettingsDao:
            - themeMode, themePackId (key-value store for extensibility)
      - [x] Update ThemeModeController to persist theme:
            - New ThemeController with ThemeState (themeMode, currentPackId, themes)
            - load persisted theme on app start
            - support system theme option with brightness detection
            - save changes to database
      - [x] Create LocalPreferencesRepository
      - [x] Build main SettingsScreen:
            - Account section (authenticated only)
            - Appearance section
            - App section
            - Account management (sign out)
      - [x] Build ThemeSettingsScreen:
            - theme mode selector (Light, Dark, System)
            - preview cards showing appearance
            - export theme JSON button
      - [x] Build AboutScreen:
            - app info (name, version, build number)
            - links (website, GitHub, Tangled, bug reports)
                  - https://lazurite.stormlightlabs.org
                  - https://github.com/stormlightlabs/lazurite
                  - https://tangled.org/desertthunder.dev/lazurite
                  - https://tangled.org/desertthunder.dev/lazurite/issues
            - credits and attributions
                  - Bluesky
                  - Flutter
                  - Attributions in README.md
            - legal (licenses, privacy, terms) - leave stubs
            - tap version to copy
- [x] Phase 2 - Bluesky Preferences Infrastructure (HIGH):
      - [x] Create BlueskyPreferences table and DAO:
            - generic table with type + data (JSON) + lastSynced
      - [x] Create domain models for preference types:
            - ContentLabelPref, LabelersPref, AdultContentPref
            - FeedViewPref, ThreadViewPref, MutedWordsPref
      - [x] Create BlueskyPreferencesRepository:
            - fetch via getPreferences, parse by $type
            - update via putPreferences (send complete pref array)
            - queue failed updates in PreferenceSyncQueue
            - merge remote changes on sync
      - [x] Extend PreferenceSyncQueue to handle all pref types
      - [x] Implement preference sync on app start (authenticated)
      - [x] Create Riverpod providers for each preference type
- [ ] Phase 3 - Content Moderation UI (MEDIUM):
      - [ ] Build ContentModerationScreen:
            - grouped content labels by category
            - adult content toggle at top
            - label visibility selectors (ignore/show/warn/hide)
            - custom labelers section (add/remove)
      - [ ] Build LabelVisibilitySelector widget
      - [ ] Implement label visibility changes with sync
      - [ ] Wire content labels to post filtering logic
- [ ] Phase 4 - Feed & Thread Preferences UI (MEDIUM):
      - [ ] Build FeedPreferencesScreen:
            - feed view toggles (hide replies, reposts, quotes)
            - thread sort order selector
            - explanatory text for each option
      - [ ] Wire feed view prefs to feed rendering
      - [ ] Wire thread view prefs to thread display
- [ ] Phase 5 - Muted Words (MEDIUM):
      - [ ] Build MutedWordsScreen:
            - list of muted words with metadata
            - add button with dialog
            - remove functionality
            - search/filter for large lists
      - [ ] Build add muted word dialog:
            - text field for word/phrase
            - target selection (content, tags)
            - actor target (all, exclude-following)
            - optional expiration date picker
      - [ ] Wire muted words to post filtering logic

Tests:
- [x] Theme persistence: mode persists across app restarts (11 DAO tests + 13 controller tests)
- [ ] System theme: follows OS brightness changes
- [ ] About screen: version info copies to clipboard
- [ ] Preference sync: Bluesky preferences sync on login
- [ ] Multi-device: preference changes merge correctly
- [ ] Offline queue: preference updates retry on network restoration
- [ ] Content filtering: labels filter posts per user preferences
- [ ] Feed filters: hide replies/reposts/quotes work correctly
- [ ] Thread sort: sort order applies and persists
- [ ] Muted words: keywords filter content from feeds

Deliverables:
- [x] Theme mode persists and supports system theme
- [ ] About screen with app info, credits, licenses, links
- [ ] Content moderation settings sync with Bluesky
- [ ] Content labels filter posts according to preferences
- [ ] Feed and thread preferences apply to display
- [ ] Muted words filter content from feeds
- [ ] All preference changes sync bidirectionally
- [ ] Offline changes queue and retry
- [ ] Multi-device changes merge without data loss
- [ ] Settings UI follows Material Design patterns
- [ ] Test coverage >95% for new code

================================================================================
L. Notifications                                                        *bsky-L*
================================================================================

Endpoints:
- app.bsky.notification.listNotifications (GET)
- app.bsky.notification.getUnreadCount (GET)

Tasks:
- Routes to add:
      - [ ] /notifs/t/:postKey  (ThreadPage reuse)
      - [ ] /notifs/u/:did      (ProfilePage reuse)
- Screens to add:
      - [ ] NotificationsPage (Notifications tab: paged list + local seen state):
            - [ ] list notifications
            - [ ] local seen state
            - [ ] tap routes to thread/profile
- Widgets to add:
      - [ ] NotificationRow (icon + actor + snippet + time)
      - [ ] UnreadDot / SeenStyle
      - [ ] NotificationFilterChips (optional: replies/mentions/reposts)
- [ ] Badge count support (in-app + push).
- [ ] Tapping a notification routes to thread/profile as appropriate.

Tests:
- [ ] Unread count updates and "mark read" local behavior.

Deliverables:
- Notifications shipped with consistent navigation.
- Notifications tab becomes meaningful
- Badge integration

================================================================================
M. DMs (read + write) + outbox + read-state                             *bsky-M*
================================================================================

DM fundamentals:
- Chat endpoints are called via user’s PDS and proxied to did:web:api.bsky.chat
  via service proxy header, per chat endpoint docs and XRPC proxy spec.

Endpoints:
- chat.bsky.convo.listConvos (GET)
- chat.bsky.convo.getMessages (GET)
- chat.bsky.convo.sendMessage (POST)
- chat.bsky.convo.acceptConvo (POST)
- chat.bsky.convo.updateRead (POST)

Tasks:
- [ ] Dio proxying:
      - add `atproto-proxy` header for all chat.* calls (service proxying).
- [ ] Drift tables:
      - dm_convos, dm_members, dm_messages
      - dm_outbox (local queued sends)
- [ ] Inbox UI:
      - convo list, unread indicators, accept requests flow
- [ ] Convo UI:
      - message list paging
      - compose/send
      - mark read on view (updateRead)
- [ ] Outbox worker:
      - persist pending messages immediately
      - retry sends safely (bounded; user-visible failure state)
      - never double-send without explicit server acknowledgement

Tests:
- [ ] Proxy header unit test: chat.* requests include atproto-proxy.
- [ ] Outbox tests: enqueue -> send -> mark delivered; retry on transient errors
- [ ] UI tests: offline send creates pending bubble; online later resolves.

Deliverables:
- DMs usable end-to-end: read, accept, send, read-state, robust retries.

================================================================================
N. Hardening (mobile)                                                   *bsky-N*
================================================================================

Tasks:
- [ ] Performance:
      - image caching strategy; list virtualization sanity
      - DB query profiling; add indexes as needed
- [ ] Accessibility:
      - semantic labels, dynamic type, contrast checks
- [ ] Moderation basics:
      - respect labels/filters returned by AppView/PDS (foundation)
- [ ] Observability hooks:
      - [x] structured logging
      - crash reporting integration points (optional)

Tests:
- [ ] Scroll perf sanity test on mid-range Android emulator profile.
- [ ] A11y audit checklist pass.

Deliverables:
- Mobile app feels stable and responsive for daily use.

================================================================================
Initial Release (Lazurite-0.1.0-alpha)
================================================================================

*todo*

================================================================================
O. Smart folders (local-only saved views)                               *bsky-O*
================================================================================

Definition:
- A smart folder is a saved query over cached posts + local metadata.
- It never fetches from network by itself; it organizes what’s already cached.

Tasks:
- [ ] Drift tables:
      - smart_folders (name, sortMode)
      - smart_folder_rules (type, payloadJson)
      - smart_folder_items materialization
- [ ] Rule engine (MVP rule types):
      - author allow/deny (DIDs)
      - keyword include/exclude (text)
      - has media / has link
      - bookmarked only
      - exclude replies / replies only
- [ ] UI:
      - create/edit folder
      - folder list and folder detail view

Tests:
- [ ] Rule evaluation tests against fixture posts.
- [ ] Query performance test with N=10k cached posts.

Deliverables:
- "Saved views" that feel like feeds, entirely local and fast.

================================================================================
P. Desktop pass                                                         *bsky-P*
================================================================================

Tasks:
- [ ] Responsive layout:
      - multi-column (timeline + thread) for wide screens
- [ ] Keyboard shortcuts:
      - navigate, like, repost, reply, new draft
- [ ] Storage:
      - confirm Drift backend performance on desktop targets
- [ ] Window behaviors:
      - restore last route, remember split pane sizes
- [ ] ThemeInspectorPane with role inspector in Settings

Tests:
- [ ] Desktop smoke tests: app boots, auth works, DB works.

Deliverables:
- Desktop build that is genuinely usable (not just "it runs").
